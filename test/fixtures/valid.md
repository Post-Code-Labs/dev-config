# Markdown fixture

This document verifies that the shared configuration loads in markdownlint-cli2.

## Structure

The formatting is intentionally simple; the smoke test is for configuration compatibility.
